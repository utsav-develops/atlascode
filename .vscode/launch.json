// A launch configuration that compiles the extension and then opens it inside a new window
// Use IntelliSense to learn about possible attributes.
// Hover to view descriptions of existing attributes.
// For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Extension",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "outFiles": [
                "${workspaceFolder}/build/**/*.js"
            ],
            "preLaunchTask": "npm: devcompile",
            "envFile": "${workspaceFolder}/.env",
            "debugWebviews": true,
            "debugWebWorkerHost": true
        },
        {
            "name": "Extension (RovoDev BBY)",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "outFiles": [
                "${workspaceFolder}/build/**/*.js"
            ],
            "env": {
                "ROVODEV_PORT": "8899",
                "ROVODEV_ENABLED": "true",
                "ROVODEV_REBRAND_JCA": "true",
                "ROVODEV_BBY": "true",
                "ROVODEV_SANDBOX_ID": "bby_sandbox",
                "ROVODEV_SERVE_SESSION_TOKEN": "bby-dev-session-token"
            },
            "preLaunchTask": "Build and Start RovoDev",
            "debugWebviews": true,
            "debugWebWorkerHost": true
        },
        {
            // This configuration requires npm run dev to be running externally
            // It will pick up the re-compiled extension code but not hot-swap it 
            "name": "Extension (watch/external)",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "outFiles": [
                "${workspaceFolder}/build/**/*.js"
            ],
            "envFile": "${workspaceFolder}/.env",
            "debugWebviews": true,
            "debugWebWorkerHost": true
        }
    ]
}
